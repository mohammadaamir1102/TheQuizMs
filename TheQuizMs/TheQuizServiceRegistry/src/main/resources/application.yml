server:
  port: 8761
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false # for avoiding itself(service registry) register because it is already a server
    fetch-registry: false
spring:
  application:
    name: SERVICE-REGISTRY
